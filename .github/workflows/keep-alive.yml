name: Keep Backend Alive

on:
  schedule:
    - cron: "*/14 * * * *" # Runs every 14 minutes to prevent 15-min inactivity sleep
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -I https://finalyearng.onrender.com

